// import { StatRecord } from "./BikeImgCornerStatRecord"
// import './BikeStats.scss'

// export const StatRecordList = ({ bike }) => {
//     const statsToRender = Object.keys(bike.ingameStats)
//     return (
//         <div className="ingame-stat-container">
//             {statsToRender.map((stat, index) => <StatRecord key={index} name={bike.ingameStats[stat].displayName} value={bike.ingameStats[stat].stringValue} />)}
//         </div>
//     )
// }